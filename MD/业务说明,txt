五、完整页面清单
用户侧
1. 登录/注册页 /login
功能：
- 账号密码输入
- 判断身份
  - admin → 跳转 /admin/dashboard
  - student → 检测是否完成Onboarding
    - 未完成 → 跳转 /user/onboarding
    - 已完成 → 跳转 /user/home
2. Onboarding引导页 /user/onboarding
触发条件：首次登录，未完成画像

第1步：基本信息
- 学校
- 专业
- 年级/毕业年份

第2步：技能概况
- 已掌握技能
- 每项技能熟练程度（入门/熟悉/熟练）

第3步：意向方向
- 想做什么方向
- 或选择"还不确定"

第4步：预期时间
- 目标达成时间（deadline）
- 每天可投入学习时长

完成后：
→ ProfileAgent 生成初级画像
→ MatchAgent 推荐3个匹配岗位（按匹配度排序）
→ 用户选择目标岗位
→ PlannerAgent 生成学习路径 + 日程表
→ 跳转主页面
3. 主页面 /user/home
每次登录的落地页

模块：
├── 今日任务
│   ├── 主线任务（岗位驱动，优先完成）
│   └── 支线任务（自选学习，完成主线后解锁）
├── 本周日程表
├── 资讯板块入口
├── 右上角头像浮窗
└── 右下角AI浮窗（全局常驻）

AI浮窗上下文：
"今天的任务你要开始了吗？"
聚焦今日任务和学习计划
4. 资讯页 /user/news
Tab切换：
├── 行业动态（SearXNG实时抓取）
├── 技术趋势（每周Agent更新）
└── 招聘信息（管理员手动发布）

AI浮窗上下文：阅读助手
- 解释文章中不懂的名词
- 总结文章内容
- 临时对话，不存入知识库
- 不影响用户画像
5. 学习路径页 /user/learning
Tab切换：

【岗位路径Tab（主线）】
- 目标岗位标签（可点击更换）
- 当前匹配度进度条
- 预计达成日期
- 阶段时间线：
  阶段一（完成）✓
  阶段二（进行中）← 当前
  └── 技能点列表
      ├── 讲义（Markdown渲染）
      ├── 多模态学习资源
      │   ├── 选择题
      │   ├── 填空题
      │   ├── 编程题（Monaco Editor）
      │   ├── 作文题
      │   └── 知识图谱（React Flow）
      ├── 通用技能考试
      │   ├── 通过 → 知识点完成 → 路径进度推进
      │   └── 未通过 → 错题分析 → 针对性学习计划
      │              → 一周内限次重考
      └── 阶段全部完成 → 解锁岗位考试
  阶段三（未开始）

【自选学习Tab（支线）】
- 顶部模糊描述输入框
  "告诉AI你想学什么..."
- AI追问澄清
- 推荐方向卡片
- 推荐课程和资源列表
- 加入日程按钮

AI浮窗上下文：
- 岗位路径Tab → 感知当前阶段和技能点
- 自选Tab → 就是输入框本身
6. 岗位页 /user/jobs
岗位列表（按匹配度排序）
匹配精准度分层：
├── 有画像有简历 → 精准匹配
├── 只有画像无简历 → 宽泛匹配（提示"完善简历提升精准度"）
└── 小白 → 潜力匹配（提示"完善信息后匹配更准确"）

岗位卡片：
├── 岗位名称
├── 匹配度百分比
└── 技能差距标签

岗位详情（点击展开）：
├── 完整JD内容
├── 已具备技能 ✓
├── 缺少技能 ✗（可一键加入学习路径）
├── 生成针对此岗位的简历
│   ├── ResumeAgent根据用户画像+项目经历生成
│   ├── 导出PDF
│   └── 一键投递（提交到管理端）
└── 参加岗位考试入口

AI浮窗上下文：
"这个岗位你还差XX技能，要加入学习路径吗？"
7. 个人信息页 /user/profile
用户画像展示：
├── 基本信息（学校/专业/年级）
├── 技能列表（含熟练程度）
├── 目标岗位
└── 学习历史记录

项目经历模块：
├── GitHub导入（计算机类）
│   └── 详见下方「GitHub导入功能详细设计」
└── 自由填写（所有专业通用）
    ├── 项目名称
    ├── 项目描述
    ├── 用到的技能
    ├── 担任角色
    ├── 时间周期
    └── 成果附件/链接

数据流向：
项目经历 → 自动补充用户画像 → 简历生成时作为数据来源

AI浮窗上下文：
"你的画像还有XX没填，完善后匹配度更准确"
GitHub导入功能详细设计
核心思路：不需要 git clone，全部通过 GitHub API 完成，轻量高效。

【数据采集分层】

第1层：API 元数据（零成本，一次调用）
GET /repos/{owner}/{repo}
├── name, description, language
├── topics（技术标签）
├── stargazers_count, forks_count
├── created_at, pushed_at（活跃度）
└── license

第2层：关键文件内容（按需读取，3-5个文件）
GET /repos/{owner}/{repo}/contents/{path}
├── README.md          → 项目描述、功能列表、截图
├── package.json       → 前端技术栈（Node.js项目）
├── pyproject.toml     → Python技术栈
├── Cargo.toml         → Rust技术栈
├── go.mod             → Go技术栈
├── docker-compose.yml → 部署方式
└── .github/workflows/ → CI/CD 工程化程度

第3层：目录树（快速判断项目类型和规模）
GET /repos/{owner}/{repo}/git/trees/{sha}?recursive=1
├── src/components/    → 前端项目
├── src/routes/        → 后端项目
├── frontend/+backend/ → 全栈项目
├── tests/             → 有测试 → 工程质量高
└── docs/              → 文档完善度

【Agent工作流（ProfileAgent处理）】

用户输入 GitHub URL
    ↓
后端接口：POST /api/user/github/analyze
    ↓
1. 解析 URL → 提取 owner/repo
2. 调用 GitHub API 拉取三层数据
3. 组装 prompt → ProfileAgent 分析
4. Agent 输出结构化 JSON
    ↓
前端展示解析结果（用户可编辑确认）
    ↓
确认保存 → 写入 MongoDB 用户画像

【Agent输出结构】

{
  "project_name": "项目名称",
  "description": "一句话描述",
  "tech_stack": ["React", "FastAPI", ...],
  "project_type": "web-app | cli-tool | library | api | mobile-app",
  "highlights": ["亮点1", "亮点2", "亮点3"],
  "activity": {
    "commits": 342,
    "contributors": 3,
    "last_active": "2026-06-01",
    "stars": 12,
    "forks": 5
  },
  "inferred_role": "核心开发者 | 贡献者 | 学习者",
  "confidence": 0.85
}

【关键文件优先级】

⭐⭐⭐ README.md          — 最重要，通常包含项目描述、功能、截图
⭐⭐⭐ package.json等      — 精确依赖 → 技术栈提取
⭐⭐   目录树 git/trees   — 项目类型、规模、架构风格
⭐⭐   .github/workflows  — CI/CD → 工程化程度
⭐     LICENSE            — 开源规范度
⭐     Dockerfile         — 部署复杂度

【前端交互流程】

点击"GitHub导入"
    ↓
输入框：粘贴仓库链接（支持 github.com/owner/repo 格式校验）
    ↓
点击"解析" → loading状态（"正在分析仓库..."）
    ↓
展示解析结果卡片：
┌────────────────────────────────────┐
│ 📦 ZhiPath                        │
│ 人岗匹配与学习路径系统              │
│                                    │
│ 技术栈：[React] [FastAPI] [Neo4j]  │
│ 活跃度：⭐12  🍴5  📝342 commits   │
│                                    │
│ 项目亮点：                         │
│ • 多Agent调度架构设计               │
│ • 知识图谱可视化                    │
│                                    │
│ 推测角色：核心开发者                 │
│                                    │
│ [重新解析]  [编辑]  [确认添加]      │
└────────────────────────────────────┘
    ↓
用户可编辑任意字段 → 确认保存
    ↓
写入用户画像 → 自动补充技能列表

【后端接口】

POST /api/user/github/analyze
请求：{ "repo_url": "https://github.com/owner/repo" }
响应：{ "code": 200, "data": { 结构化JSON } }

POST /api/user/projects/save
请求：用户编辑确认后的完整项目数据
响应：{ "code": 200, "message": "保存成功" }

【GitHub API 注意事项】

- 未认证：60次/小时，够用但紧张
- 后端配置 Personal Access Token：5000次/小时
- 无需 clone：全部 GET 请求
- 缓存策略：同一仓库 24h 内不重复分析（Redis缓存）

【数据流向对接】

项目经历保存
    ↓
ProfileAgent 更新用户画像（MongoDB）
├── 技能列表自动补充（从 tech_stack 提取）
├── 项目经历写入 profile.projects
└── Neo4j 技能节点关系更新
    ↓
简历生成时自动引用（ResumeAgent）
    ↓
岗位匹配时作为加分项（MatchAgent）
8. 头像浮窗（全局右上角）
点击头像弹出：
├── 个人信息 → /user/profile
├── 我的学习路径 → /user/learning
├── 目标职位（可重新匹配）→ /user/jobs
├── 设置
└── 退出登录

换目标岗位逻辑：
- 已掌握技能点进度全部保留
- AI分析新旧岗位差异
- 追加新技能到学习路径
- 不再需要的技能归档到"已学技能"
- 学习路径平滑更新，不推倒重来
9. AI浮窗（全局右下角常驻）
形态：
├── 默认：右下角悬浮按钮
├── 点击展开：小浮窗（聊天气泡模式）
└── 点击放大：全屏对话模式（可缩回）

上下文感知（按页面独立session）：
├── 主页面 → 今日任务助手
├── 学习路径页（岗位Tab）→ 学习进度助手
├── 学习路径页（自选Tab）→ 学习推荐助手
├── 岗位页 → 岗位分析助手
├── 个人信息页 → 画像完善助手
└── 资讯页 → 阅读理解助手（临时，不存库）

技术实现：
- 全局用户画像 → MongoDB（所有页面共享）
- 各页面对话历史 → Redis（按页面区分session）
管理端
1. 首页看板 /admin/dashboard
数据卡片：
├── 活跃用户数 / 新增用户趋势
├── Token消耗量 + 费用估算
├── 今日匹配次数
└── Agent调用次数分布

系统状态：
└── 各中间件运行状态
    Redis / Neo4j / Chroma / Ollama /
    MySQL / MongoDB / RabbitMQ / MinIO / SearXNG
2. 用户管理 /admin/users
用户列表
├── 搜索/筛选
├── 用户详情（画像/学习进度/匹配记录）
└── 禁用/启用账号
3. 岗位管理 /admin/jobs
岗位列表
├── 手动录入/粘贴JD
│   └── 触发 JDParserAgent 自动解析
│       ├── 提取岗位名称
│       ├── 提取必须技能
│       ├── 提取加分技能
│       └── 更新 Neo4j 知识图谱
├── 查看解析结果
└── 上架/下架岗位

实时更新机制：
├── 管理员手动录入JD（第一版）
├── SearXNG定时抓取新岗位（第二版）
└── 企业自助发布（第二版）
4. 企业管理 /admin/enterprise
企业入驻审核
├── 查看入驻申请
├── 审核通过/拒绝
└── 企业联络员邮箱管理
5. 简历投递处理 /admin/resumes
投递列表（待处理）
├── 用户基本信息
├── 目标岗位
├── ReviewerAgent筛选建议
│   └── 建议通过/拒绝 + 原因（仅供参考）
└── 管理员最终决策
    ├── 通过 → 发送至企业联络员邮箱
    └── 拒绝 → 通知用户原因
6. 考试题库管理 /admin/exams
通用技能考试题库：
├── Agent自动出题
├── 管理员审核
└── 手动编辑/录入

岗位考试题库：
├── 企业真题录入
├── Agent参考真题风格生成
├── 管理员用企业题目校准
└── 上架/下架题目
7. 知识图谱管理 /admin/graph
Neo4j图谱可视化
├── 技能节点查看
├── 岗位关系查看
└── 手动修正错误节点
8. 资讯管理 /admin/news
发布招聘信息
├── 标题/内容/链接
└── 上架/下架
9. 系统配置 /admin/settings
├── Ollama模型切换
├── 各Agent开关
└── Token限额设置
六、核心业务流程
知识库运作机制
LLM生成讲义大纲
↓
存入知识库（全平台复用，非per用户）
↓
Agent发散生成多模态资源：
├── 选择题 / 填空题 / 编程题 / 作文题
└── 知识图谱数据（Neo4j → React Flow渲染）
↓
ReviewerAgent 质量审查过滤
↓
每周定期更新：
SearXNG搜索最新行业资料
→ 与知识库内容对比
→ 发现出入 → 标记待审查
→ Agent结合LLM判断是否更新
→ 更新知识库内容
考试机制
【通用技能考试】
Agent出题 → 管理员审核 → 上架题库
↓
用户完成该技能点学习后参加考试
↓
通过 → 知识点掌握 → 路径进度推进 → 可参与达标考试
未通过 → ReviewerAgent分析错题
       → 自动生成针对性学习计划
       → 一周内限次重考

【岗位考试】
企业真题 + Agent生成 → 管理员校准审核
↓
用户完成阶段学习后解锁
↓
通过 → 匹配度大幅提升 → 可投递简历
未通过 → 同通用考试机制
学习进度确认机制
看完讲义 → 自动记录阅读进度
↓
完成习题且批改通过 → 知识点完成
↓
积累足够知识点 → 解锁阶段考试
↓
通过阶段考试 → 该阶段完成 → 匹配度提升
今日任务优先级机制
主线任务（岗位驱动）
└── 每天至少完成一个
    完成后 ↓
支线任务（自选学习）
└── 主线完成后解锁当天支线

总时长控制：
└── 不超过用户设定的每日学习时长
    岗位任务优先填满时间
换目标岗位机制
用户更换目标岗位
↓
AI分析新旧岗位差异
↓
已掌握技能 → 全部保留（技能属于用户不属于岗位）
新岗位需要的技能 → 追加到学习路径
不再需要的技能 → 归档到"已学技能"
↓
学习路径平滑更新
简历投递闭环
用户在岗位页生成针对性简历
↓
一键投递 → 提交到管理端
↓
ReviewerAgent初步分析
└── 给出筛选建议（建议性质，决策权在人）
↓
管理员查看建议 → 做最终决策
├── 通过 → 发送至企业联络员邮箱
└── 不通过 → 通知用户原因
知识图谱实时更新机制
新岗位录入（任意来源）
↓
RabbitMQ接收异步任务
↓
JDParserAgent解析JD
├── 提取技能节点
└── 建立技能-岗位关系
↓
Neo4j自动更新
↓
现有用户匹配度重新计算
↓
相关用户收到通知："有新岗位匹配你"
GitHub项目导入机制
用户粘贴 GitHub 仓库链接
    ↓
后端解析 URL → 提取 owner/repo
    ↓
并行调用 GitHub API：
├── GET /repos/{owner}/{repo}           → 元数据
├── GET /repos/.../contents/README.md   → 项目描述
├── GET /repos/.../contents/package.json等 → 技术栈
└── GET /repos/.../git/trees/{sha}      → 目录结构
    ↓
组装上下文 → ProfileAgent 分析
├── 提取技术栈
├── 推断项目类型和规模
├── 生成项目亮点描述
└── 推测用户角色
    ↓
返回结构化结果 → 用户编辑确认
    ↓
保存到 MongoDB 用户画像
├── 项目经历写入 profile.projects
├── 技能列表自动补充
└── Neo4j 技能关系更新
    ↓
下游联动：
├── MatchAgent 匹配度重新计算
├── ResumeAgent 简历数据源更新
└── 学习路径可能调整（已掌握技能变化）

缓存策略：同一仓库 24h 内缓存在 Redis，不重复调用 API
频率控制：后端配置 GitHub Token，5000次/小时
七、前端页面开发顺序
原则：每个页面独立开发，全部使用假数据，能看到效果再进行下一个

第一批：地基
1. 项目初始化
   Vite + React + TypeScript
   安装所有依赖
   配置路由入口

2. 登录页 /login
   账号密码表单
   假数据身份判断
   admin → /admin/dashboard
   student → /user/home
第二批：用户端核心流程
3. 用户端整体框架
   右上角头像浮窗
   右下角AI浮窗（先做UI不接逻辑）
   路由配置

4. Onboarding /user/onboarding
   四步分步表单
   假数据提交直接跳主页面

5. 主页面 /user/home
   今日任务卡片（主线+支线）
   本周日程表
   资讯入口
   全部假数据

6. 学习路径页 /user/learning
   Tab切换
   岗位路径时间线
   技能点展开
   多模态资源占位组件
   自选学习输入框
   全部假数据
第三批：用户端次要页面
7. 岗位页 /user/jobs
   岗位卡片列表
   匹配度展示
   岗位详情弹窗

8. 资讯页 /user/news
   三个Tab
   文章卡片列表

9. 个人信息页 /user/profile
   画像展示
   项目经历模块
   ├── 自由填写表单（先做）
   └── GitHub导入（先做UI，第五批接后端时实现解析逻辑）
第四批：管理端
10. 管理端框架
    侧边栏导航
    整体布局

11. 首页看板 /admin/dashboard
    数据卡片
    图表（假数据）
    中间件状态列表

12. 用户管理 /admin/users
13. 岗位管理 /admin/jobs
14. 企业管理 /admin/enterprise
15. 简历投递处理 /admin/resumes
16. 考试题库管理 /admin/exams
17. 知识图谱管理 /admin/graph
18. 资讯管理 /admin/news
19. 系统配置 /admin/settings
第五批：接入后端（假数据替换为真实接口）
按页面逐个替换，一次只动一个模块
每替换一个测一个
顺序：登录 → 主页面 → 学习路径 → 岗位页 → 其他

GitHub导入后端实现：
├── POST /api/user/github/analyze — 调用GitHub API + ProfileAgent解析
├── POST /api/user/projects/save — 保存项目经历到MongoDB
├── Redis缓存：同一仓库24h内不重复分析
└── 后端配置 GitHub Personal Access Token（5000次/小时）
八、注意事项
1. 前端全部做完（假数据）再接后端
2. 每个页面做完立刻git commit
3. 报错直接贴完整错误信息给AI
4. 同一个报错修超过3次换思路重写
5. 知识库是全平台复用资产，不是per用户生成
6. AI对话session按页面隔离，用户画像全局共享
7. 企业对接第一版用管理员代替手动操作
8. 学生身份为第一版主体，职场人第二版扩展