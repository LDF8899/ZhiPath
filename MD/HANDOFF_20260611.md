# 智途 ZhiPath 交接文档 — 2026-06-11

## 本次完成的工作

### 1. 数据库重建（v2 schema → 17张表）
- 数据库 `zhipath` 已用 utf8mb4_unicode_ci 重建
- 17张 v3 表：users_v3、students_v3、enterprises_v3、job_positions_v3、learning_plans_v3、learning_tasks_v3、learning_sessions_v3、user_skills_v3、exam_questions_v3、exam_records_v3、job_applications_v3、news_v3、notifications_v3、resumes_v3、operation_logs_v3、knowledge_base_v3、system_config_v3
- 完整建表脚本：`D:\X\ZhiPath\MD\zhipath_schema_v2.sql`
- 测试数据脚本：`D:\X\ZhiPath\MD\seed_test_data.sql`（7用户/5企业/8岗位/5计划/15技能/8考题等）
- dump 备份：`D:\X\ZhiPath\MD\zhipath_dump_20260611.sql`

### 2. TypeORM Entity 层对齐（14个 entity）
- 所有 entity 文件在 `backend-ts/src/entities/` 下
- 全部对齐 v2 schema（表名/列名/类型/注释）
- TS 编译零错误

### 3. Auth 模块验证（注册→登录→/me 全链路跑通）
- 后端：`backend-ts/src/modules/auth/`（controller/service/module）
- 密码：bcrypt hash
- Token：JWT（7天过期）
- 中文存储：utf8mb4 全链路OK

### 4. 数据库连接容错
- Neo4j/Redis/MongoDB 连接失败不再阻塞启动，只依赖 MySQL
- 文件：`backend-ts/src/database/neo4j.module.ts`、`redis.module.ts`、`mongodb.module.ts`
- TypeORM charset 修复：`charset: 'UTF8MB4_UNICODE_CI'`（TypeORM 传给 mysql2 的是 collation 名称）

### 5. Onboarding 全链路 ✅
- **前端**：3步向导（基本信息→技能水平含级别→方向+每日时长Slider）
- **后端**：submitOnboarding 重写 — 保存学生档案 → 写入 user_skills_v3 → 查找目标岗位 → 创建 learning_plans_v3（含 pathData JSON）→ 创建 learning_tasks_v3（第一天任务）→ 同步 MongoDB
- **计划模板**：`backend-ts/src/modules/student/plan-templates.ts`（8方向：frontend/backend/fullstack/mobile/ai/data/devops/design）
- **路由守卫**：ProtectedRoute 检查 onboardingCompleted，未完成自动重定向 /onboarding
- **API**：`POST /api/user/onboarding` 返回 `{completed, plan:{id,planName,estimatedDate,totalSkills,todayTasks}}`
- TS 编译零错误（前后端）

---

## 当前项目状态

### 目录结构
```
D:\X\ZhiPath\
├── backend-ts\          # NestJS 后端（TypeORM + MySQL + MongoDB + Redis + Neo4j）
│   └── src\
│       ├── modules\     # auth, student, dashboard, jobs, learning-paths, exams, news, graph, chat, tasks, admin, progress, knowledge, github, scheduler
│       ├── entities\    # 14个 TypeORM entity（对齐 v2 schema）
│       ├── database\    # mysql/mongodb/redis/neo4j 模块
│       └── common\      # auth.guard.ts, api-response.ts, base.entity.ts
├── frontend\            # React + Vite + Antd + Zustand
│   └── src\
│       ├── pages\       # Login, Register, Onboarding, user/*, admin/*
│       ├── stores\      # auth.ts (Zustand, sessionStorage)
│       ├── api\         # client.ts (Axios), user.ts (30+ API)
│       └── App.tsx      # 路由 + ProtectedRoute 守卫
└── MD\                  # 所有文档
```

### 中间件（Docker）
| 服务 | 端口 | 状态 |
|---|---|---|
| MySQL | 3307 | ✅ 运行中，用户 root/root123 |
| Redis | 6379 | 可选（未启动不影响核心功能） |
| MongoDB | 27017 | 可选 |
| Neo4j | 7687 | 可选 |

### 启动命令
```bash
# 后端
cd D:\X\ZhiPath\backend-ts
npm run start:dev          # 端口 3000

# 前端
cd D:\X\ZhiPath\frontend
npm run dev                # 端口 5173，需确认 Vite proxy 配置 /api → localhost:3000
```

---

## 下一步工作（按优先级）

### ~~P0：Onboarding~~ ✅ 已完成
- 前端 3 步向导 + 后端学习计划生成 + 路由守卫

### P1：Dashboard（主页）
- 后端 `modules/dashboard/` 已有
- 需要对接：今日任务（learning_tasks_v3）、学习统计、匹配度、推荐岗位、最新资讯
- 计划切换器（learning_plans_v3 多计划）

### P2：学习路径页
- 展示计划 phases + 任务状态机流转
- 任务完成操作（PENDING → IN_PROGRESS → ... → EXAM_DONE）

### P3：技能系统增强
- user_skills_v3 掌握度更新（讲义+30%、选择题+25%、编程题+25%、考试+20%）
- 衰减模型（3个月后未使用 trustWeight -0.1）

---

## 已知问题
- 前端未验证能否正常启动（Vite proxy 配置未检查）
- curl 在 bash 中发送中文会乱码，PowerShell 没问题（前端浏览器不受影响）
- 后端部分模块（chat/graph/knowledge 等）可能还是旧代码骨架，需逐个验证

---

## 关键文档
| 文件 | 内容 |
|---|---|
| `MD/业务深度设计_v2.md` | 完整业务设计（17个章节，所有模块的详细设计） |
| `MD/CONSTITUTION.md` | 业务规范宪法 |
| `MD/DEVELOPMENT_GUIDELINES.md` | 前后端开发准则 |
| `MD/zhipath_schema_v2.sql` | 建库脚本 |
| `MD/seed_test_data.sql` | 测试数据 |
| `MD/zhipath_dump_20260611.sql` | 完整 dump 备份 |

---

## 交接提示词

```
我在做智途 ZhiPath 项目，这是一个面向学生的人岗匹配+个性化学习平台。

上一轮完成了：
1. 数据库重建（17张 v3 表，utf8mb4，已有测试数据）
2. TypeORM Entity 层对齐（14个 entity，TS 编译零错误）
3. Auth 模块验证（注册/登录/me 全链路跑通，bcrypt + JWT）
4. 数据库连接容错（Neo4j/Redis/MongoDB 不阻塞启动）
5. Onboarding 全链路（前端3步向导 + 后端学习计划生成 + 路由守卫）

所有文档在 D:\X\ZhiPath\MD\ 目录下。
业务设计文档是 MD/业务深度设计_v2.md。
后端在 backend-ts/，前端在 frontend/。

下一步要做 Dashboard 主页（对接学习计划+今日任务+统计数据）。
请先读 MD/HANDOFF_20260611.md 和 MD/业务深度设计_v2.md 了解全貌，然后开始。
```
